server:
   port: 7443

spring:
   application:
      name: STATUS-SERVICE
   mvc:
      throw-exception-if-no-handler-found: true
   resources:
      add-mappings: false
   zipkin:
      base-url: http://127.0.0.1:9411/
      
      
eureka:
   client:
      register-with-eureka: true
      fetch-registry: true